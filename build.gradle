allprojects {
	apply plugin: 'java'
//	apply plugin: 'application'
	apply plugin: 'idea'
}

version = '1.0.SNAPSHOT'

project(':logic'){}

project(':repository'){
	dependencies {
		compile project(':logic')
	}
}

task wrapper (type: Wrapper) {
	gradleVersion = '2.3'
}

//mainClassName = 'com.pluralsight.gradle.Application'
